{
  "name": "auth-basic-jwt",
  "description": "JWT auth with register/login/refresh (in-memory users).",
  "tags": ["auth", "jwt"],
  "requires": [
    "JWT_ACCESS_SECRET",
    "JWT_REFRESH_SECRET",
    "JWT_ACCESS_EXPIRES_IN",
    "JWT_REFRESH_EXPIRES_IN"
  ]
}
